def primes():
    yield 2
    prime = 3
    while True:
        is_prime = True
        divisor = 3
        while divisor ** 2 <= prime:
            if prime % divisor == 0:
                is_prime = False
                break

            divisor += 2

        if is_prime:
            yield prime

        prime += 2
